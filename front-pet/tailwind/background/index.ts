import { colors } from "./color"
import { image } from "./image"
import { imagePositions } from "./imagePositions"

export const backgroundClasses = {
  image,
  imagePositions,
  colors
}

export default backgroundClasses
